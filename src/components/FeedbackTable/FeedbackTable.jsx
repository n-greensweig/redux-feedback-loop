function FeedbackTable() {

// Display the existing feedback in an MUI table,
// with the most recent feedback appearing at the top of the list
// (sorted by id DESC)


    return (
        <h1>Admin</h1>
    )

}

export default FeedbackTable;